import { makeStyles } from "@material-ui/core/styles";

const UndoStyles = makeStyles((theme) => ({
    undoIcon: {
        color: "#FFFFFF",
        width: "24px",
        height: "24px",
    },
}));

export default UndoStyles;