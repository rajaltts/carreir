import { makeStyles } from '@material-ui/core/styles';

const addProjectDialogStyles = makeStyles((theme) => ({
    ButtonsStyles: {
    "display": "flex",
    "column-gap": "10px"
    }
}));

export default addProjectDialogStyles;