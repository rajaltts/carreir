import { makeStyles } from "@material-ui/core/styles";

const RedoStyles = makeStyles((theme) => ({
    redoIcon: {
        color: "#FFFFFF",
        width: "24px",
        height: "24px",
        marginLeft: "11.53px"
    },
}));

export default RedoStyles;